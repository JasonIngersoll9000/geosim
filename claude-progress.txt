# GeoSim Progress Log
Updated by Claude Code at the end of each session.

## Current sprint: Sprint 3 — Real Data + AI Pipeline
## Last session: 2026-04-18

### Completed
- Next.js 14 scaffolding with App Router, TypeScript, Tailwind
- Supabase schema migration (supabase/migrations/20260319000000_initial_schema.sql)
- TypeScript types: lib/types/database.ts, lib/types/simulation.ts (+ TurnPlan types)
- Supabase client utilities: lib/supabase/client.ts, lib/supabase/server.ts
- Supabase middleware: middleware.ts (session token refresh)
- Vitest setup with tests passing
- All game logic (fog-of-war, escalation, TurnPlan validation, state updates)
- Research pipeline API routes (Stages 0–6)
- Iran scenario data incorporation (seed script, verified actor state)
- **Sprint 2 Stitch frontend migration — ALL 14 TASKS COMPLETE (PR #42, merged)**
  - Design tokens: Space Grotesk (font-label), Newsreader (font-serif), IBM Plex Mono (font-mono), Inter (font-sans), gold #ffba20
  - UI primitives: TopBar, ClassificationBanner, DocumentIdHeader, SectionDivider, Badge, Button, ScoreDisplay, ProgressBar, SlideOverPanel
  - Game components: EscalationLadder, ActorAvatar, DimensionTag, IntelligenceReportBlock, ConstraintCascadeAlert
  - War Chronicle: TurnEntry, GlobalTicker, ChronicleTimeline
  - Map shell: GameMap, ActorLayer, ChokepointMarker, FloatingMetricChip, MapLegend
  - Actor panels: ActorList, ActorDetailPanel, GlobalIndicators
  - Decision panels: DecisionCatalog, TurnPlanBuilder, DecisionDetailPanel
  - DispatchTerminal (turn resolution animation)
  - EventsTab, ObserverOverlay
  - Full GameView layout (two-column, all panels wired with mock data)
  - Realtime shell (useRealtime hook, GameProvider)
  - Scenario Hub page (/scenarios/[id]) with ActorCard, tab switching, navigation
  - Scenario Browser page (/scenarios) with category filter
  - All navigation wired: home → /scenarios → /scenarios/iran-2026 → /play/trunk
- **Workflow setup:**
  - Custom skill: geosim-playwright (~/.claude/skills/geosim-playwright/)
  - Custom skill: geosim-uiux-validation (~/.claude/skills/geosim-uiux-validation/)
  - CLAUDE.md: Development Workflow Rules added (branch-per-feature, playwright validation, TDD, skill selection guide)
  - CLAUDE.md: ESLint gotchas documented (JSX comment textnodes, unused params, inline style ordering)
  - GitHub issues #20–#26 closed (Stitch migration complete)
  - docs/scrum-issues.md rewritten: full Phase A–D roadmap to playable game
  - GitHub issues #27–#41 created (full remaining roadmap)

### Open PRs
- **PR #65** — feat: wire player decisions through full AI pipeline
  Branch: `docs/advance-pipeline-spec` (16 commits ahead of main)
  Status: OPEN, ready for review/merge pending DB migration + manual smoke test

### Session 2026-04-18 work (PR #65 / plan 2026-04-15-advance-full-pipeline.md)
- All 10 tasks of the full-pipeline plan complete
- `/advance` rewritten: instant ~200ms response + `@vercel/functions` `waitUntil` background pipeline
- Pipeline: actor agents (Promise.allSettled) → resolution → judge+retry → narrator → persist state
- Progress via Supabase Realtime broadcasts: `turn_started`, `resolution_progress`, `turn_completed`, `turn_failed`
- `DispatchTerminal` now renders deterministic 6-phase checklist from GameProvider state (not teletype)
- `TurnPhase` union: `submitted | planning | resolving | judging | narrating | finalizing | complete | failed`
- DB migration `20260415000000_turn_phase_enum.sql` — NOT YET APPLIED to staging/prod
- Deleted `/api/game/turn/route.ts` (folded into `/advance`)
- Added `@vercel/functions@3.4.3` dep
- New tests: `turn-helpers`, `turn-broadcast`, `DispatchTerminal` (rewritten), `advance` integration
- All typecheck + lint clean; 217/221 tests passing (4 pre-existing failures: seed-iran, middleware, research-pipeline, TurnPlanBuilder — unrelated)
- Call-site fixes: GameView passing `.id` strings (was ActionSlot objects); TopBar/TurnPhaseIndicator use new TurnPhase union; panels.`information` → simulation.`intelligence` dimension bridge

### Smoke test results (local, 127.0.0.1:3001)
- Page renders cleanly, no React runtime errors from type changes
- TurnPhase indicator renders "planning" correctly in TopBar
- Full end-to-end pipeline NOT exercised (requires deployed env): Realtime times out on localhost origin cookies; Play-as-Actor requires branch creation (issue #38 not yet implemented)

### Known follow-up (separate PR after #65 merges)
- `lib/game/state-engine.ts` (`getStateAtTurn`, `persistStateSnapshot`) uses cookie-based `createClient()` — won't work from background pipeline (no HTTP request context). First real run will surface via `turn_failed` broadcast. Fix = inject a service client.

### In progress
- Nothing — awaiting review/merge of PR #65

### Next up (in priority order)
- Issue #29: Landing page (replace component showcase with real product page)
- Issue #30: Animations pass (entrance animations + premium motion throughout)
- Issue #27: Iran scenario Supabase seed (gateway to entire real-data path)
- Issue #28: Replace mock data with Supabase queries (depends on #27)
- Issue #31–#36: AI pipeline (research → actor → resolution → judge → narrator → game loop)

### GitHub issue map (current)
- #27: Iran scenario Supabase seed
- #28: Replace mock data with Supabase queries
- #29: Landing page
- #30: Animations pass
- #31: Research pipeline
- #32: Actor agent
- #33: Resolution engine
- #34: Judge evaluator
- #35: Narrator
- #36: Game loop controller
- #37: Turn submission
- #38: Branch creation
- #39: Mapbox Tier 1
- #40: Supabase Auth
- #41: CI/CD pipeline

### Known bugs
- None

### Design debt
- getConstraintCascadeRisk (lib/game/escalation.ts) uses Iran-specific keyword matching
  instead of reading from Scenario.constraintCascades — tracked for future sprint

### Architecture decisions
- Using bun (not npm/npx) for all package management — npm/npx are Windows binaries in WSL2
- Vitest configured with node environment (not jsdom) for server-side logic tests
- docs/ excluded from TypeScript compilation (reference docs, not compiled code)
- ESLint argsIgnorePattern: ^_ must be in .eslintrc.json (Vercel build requires it)
- prefer-const applies even when mutating object properties via bracket notation
- Concurrent TurnPlan validation must check every concurrent pair (i<j loop), not just vs primary
- JSX comment text: use {' // text '} not // text (react/jsx-no-comment-textnodes ESLint error)
- Inline style objects: put border shorthand BEFORE borderBottom, not after (shorthand overwrites specific)
- Frontend validation workflow: geosim-playwright skill → geosim-uiux-validation skill → user approves all 4 CATs → commit
